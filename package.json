{
  "name": "gameapp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "web": "cocos run -p web",
    "a": "cocos run -p android -j4",
    "mac": "cocos run -p mac",
    "ios": "cocos run -p ios"
  },
  "dependencies": {
    "underscore": "^1.8.3",
    "grunt": "^0.4.5",
    "grunt-shell": "^1.1.2",
    "grunt-sync": "^0.2.4"
  }
}
